<div onclick={{action "open"}}>
  {{d-icon "link"}}
  {{group}}
</div>
{{#if visible}}
  <div class='popup'>
    <label>{{i18n "explorer.link"}} {{group}}</label>
    <input value={{link}}/>
    <span onclick={{action "close"}}>
      {{d-icon "times"}}
    </span>
  </div>
{{/if}}
